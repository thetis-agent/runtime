# Decisions taken

- 2026-09-10: The authorized runtime review remediation passes the full mandatory-sandbox suite: 455/455 tests, none failed, skipped or cancelled. Strict checking, lint and generated/execution-artifact freshness pass. Idle RSS is 159,289,344 bytes against 512,000,000 and watched-edit latency is 1,591.96 ms against 2,000 ms. The unchanged kernel-size gate remains red at 1,680 counted lines against 1,300. No commits or vendor calls are made. A separate npm reuse assessment identifies support-library opportunities but no package substitution that closes the kernel overage.
- 2026-09-10: The operator permits edit latency up to two seconds and authorizes the runtime review's P1 and P2 fixes. Accepted ADR 0042 changes the watched edit-to-serve gate to at most 2,000 milliseconds, superseding the earlier subsecond threshold while retaining the same workload and mandatory sandbox. ADRs 0043–0046 record generation recovery, exact provider accounting, dynamic registration recapture and ephemeral resource retirement.
- 2026-09-10: The operator explicitly permits memory up to 512 MB and requests raising the ceiling. Accepted ADR 0041 changes aggregate kernel-plus-idle-environment RSS acceptance to at most 512,000,000 bytes, preserving the existing decimal units and measured processes. The focused sandboxed test passes at 156,164,096 bytes. The preceding full review suite passed 415/417 tests; its old memory gate and 1,572.67 ms latency failed. The one-second edit-to-serve ceiling and individual sandbox limits remained unchanged at this point; ADR 0042 subsequently amends latency. Earlier 120 MB results are historical measurements under the former gate.

- 2026-09-09: The operator requests a storage contract and a default file or SQLite package. ADR 0040 chooses files to preserve existing encrypted-object and journal formats. `contract/storage` specifies bounded byte objects and durable append logs; `storage-files` exports the shared `lib/storage` factory, which is also the kernel's fixed reviewed default. The kernel retains encryption, scope and grant checks, and observed provenance. The extraction removes 15 counted kernel lines; with concurrent boundary work the current total is 1,583. The full suite reports 399/403 passes (two performance and two web-asset failures); 23 final focused tests pass after testing copied limits, bounded object operations, detached input buffers, actual I/O refusal and recovery. The full strict check remains blocked by concurrently edited gateway-web files. The default profile includes all three new storage pins; no release is published and no gate is relaxed.

- 2026-09-09: The operator permits excluding comments from the kernel size budget (ADR 0039). The gate now reports physical, comment-only and counted lines; code sharing a line with a comment and blank lines outside comments still count. The current inventory is 1,615 physical minus 19 comment-only = 1,596 counted lines, still above 1,300. Earlier physical-line measurements remain historical results under the previous accounting rule.

- 2026-09-09: Final ADR 0037 validation passes strict checking and freshness, with 354 of 356 tests passing and none skipped. The remaining measured failures are 155,230,208 bytes RSS against 120,000,000 and 1,499.55 ms edit-to-serve against 1,000. The kernel is 1,610 physical lines against 1,300; installer, seed review and clause-level audit remain. Two fresh 52-pin releases are byte-identical. No paid check is repeated and no source-repository commit is made. Milestone A remains unaccepted.
- 2026-09-09: Both source repositories run the same complete CI gate in the ADR 0035 layout. Delivery builds require an existing main-history tag and an exact reviewed peer commit, retain source/output provenance, verify a fresh offline registry and publish all matching assets together. GitHub Release delivery leaves evaluator evidence and code-confirmed activation in the kernel (ADR 0014, ADR 0018, ADR 0037). Existing acceptance failures remain blocking; no deployment guarantee is changed.

- 2026-09-09: TE-023 constructs its shutdown handler dynamically; under ADR 0037 it must build that fixture before pinning it. The initial full artifact run correctly refused the unbuilt handler. Both shutdown modes keep their real hook, surviving-child, injected-deadline and fencing assertions; no test is skipped or weakened.
- 2026-09-09: Registry assembly reuses immutable cached blobs through hard links instead of duplicating them for every profile. Only the registry-owned, hash-checked cache uses this path; ordinary materialization, kernel generation snapshots and mutable state still make isolated copies. The linked result is hashed again, and the kernel copies and checks every new pin before admission. This addresses artifact-related tmpfs exhaustion without relaxing quotas or permitting candidate writes to a shared cache.
- 2026-09-09: ADR 0037 verifies each advertised source/output pair on the copied pin before mounting, including retained pins. Vendored JavaScript distributions also ship non-executable TypeScript sources without generated pairs; they remain ordinary pinned data. Every attempted TypeScript import still requires verified output, with Node's implicit type stripping disabled, so removing both sidecars cannot activate source code. Work builds reuse unchanged output only from the previous trusted pin, never from editable sidecars.
- 2026-09-09: Generated artifacts increase loose git-object and tmpfs page costs. Registry bootstrap incrementally packs each reviewed publication with one compression worker and no delta search, retaining refs and immutable object ids. This reduces physical overhead without increasing the 128 MiB sandbox memory limit, changing logical retention or running package code; see the [git repack specification](https://git-scm.com/docs/git-repack).
- 2026-09-09: The operator explicitly accepts ADR 0037 after reviewing the no-build tradeoff. Implement deterministic Node type erasure with per-file source/output hashes and exact Node version. A synchronous, reviewed preload supplies verified JavaScript under the original TypeScript URL, preserving imports, worker entries and source positions without retaining the TypeScript parser in serving processes. Artifact execution is explicit in neutral process plans; legacy source plans remain supported, but artifact plans never fall back on missing or mismatched output. Work compilation runs in a bounded child, before generation admission, and is included in edit latency. Bootstrap loader generation and schema-validator output have freshness checks.
- 2026-09-09: The latest operator confirmation defers UI only. Memory is measured rather than treated as a waiver: the real kernel and one idle sandbox remain above 120 MB. The sub-second edit assertion also remains red. ADR 0037 proposes, but does not implement, a change to ADR 0002's no-build-step guarantee; the operator's decision is pending.
- 2026-09-09: The explicitly authorized OpenRouter check completes four real answer turns across two separately sandboxed accounts, with a combined configured cost ceiling of USD 0.04. Read only `llm.api_key` from the operator's `/opt/thetis/thetis.local.toml` through private inherited descriptors; no raw key enters argv, configuration, the repository or logs. Keep this paid check separate from the offline suite.
- 2026-09-09: Static manifest requirements are not newly registered requirements and must not be tested as dynamic envelope additions. `kernel/secrets/spawn.ts` retains declared-secret validation and refuses explicit requirements outside the envelope; regression tests cover both cases (KS-009, PR-013).
- 2026-09-09: ADR 0036 permits only `.node-compile-cache` as the additional discovery-state entry. TE-021 tests still require identical registrations, no failures and no other state entries; the expected directory list changes explicitly rather than skipping either conformance test. Native cache readiness diagnostics travel only through environment health metadata, never kernel startup stdout.
- 2026-09-09: GN-004 and GN-006 still assert the exact discarded application paths, with additional cache-file paths explicitly recognized under ADR 0036. Production rollback/undo observations continue to list every changed cache file; no audit row or snapshot entry is filtered out to make tests pass.
- 2026-09-09: Export the current normalized source closure into a real offline git bundle and exact default profile pins. Independently unbundle it in a fresh registry, inspect every commit, verify each tree hash, assemble the closure and reject a changed hash. This is a reproducible package artifact, not yet an approved kernel installation or provisioned persistent deployment.
- 2026-09-09: Fix release packing at one git compression worker. Two exports already produced identical pins but different pack bytes under automatic threading; two fresh single-worker exports now produce byte-identical manifests, locks, checksums and bundles. Bundle hashing streams under a named 64 MiB limit.
- 2026-09-09: Relocate the runtime checkout to `/tank/data/Dev/thetis-agent/runtime` after checksum comparison, preserving dirty files, the git index and history. Keep `/opt/zero` as a temporary compatibility symlink and `/opt/zero.pre-relocation-20260909` as the untouched backup; the link is not an installed release. Strict checks and all 23 focused tests pass from the new path using sibling package discovery.
- 2026-09-09: ADR 0035 keeps runtime and package repositories separate, with portable read-only development mounts in the installed layout. Generated package imports stay relative; repository metadata is ignored during plain-package discovery. The intended runtime source location is `/tank/data/Dev/thetis-agent/runtime`; `/opt/zero` remains the installation target.
- 2026-09-09: Restore registry fixtures' explicit schema initialization and let committed-schema programming errors escape `Registry.open`; they are not missing-registry I/O errors. Keep synchronous lazy socket parameter validation and reject async schemas.
- 2026-09-09: Remove temporary startup measurements from kernel stdout so its readiness frame remains one valid JSON document. Bind transient execution setup to its generation-owned endpoint before probing and repointing, and negotiate session methods while refusing candidate-originated upstream routing. The production evaluator fixture stops its preparation-only provider before starting a kernel that reaps its dedicated cgroup subtree (EV-006).

- 2026-09-09: The operator extracted packages into a separate git registry at `/tank/data/Dev/thetis-agent/packages` (`thetis-agent/packages`). This tree reviews that checkout through `THETIS_PACKAGES` and no longer stores package sources.
- 2026-09-09: The operator deferred memory/storage-size profiling acceptance and visual gateway UI work; retain all sandbox, quota and secret guarantees while prioritizing headless deployment completion.
- 2026-09-09: Encode an unassigned deployment principal as the empty person string required by the kernel socket schema; it names no account and cannot access conversations. Personal sidecars resolve only matching logical run ids, target, person and generation (KS-022).

- 2026-09-09: Use installed Node 24.18.0 for direct TypeScript execution; the shell's Node 18 is unsuitable (ADR 0002).
- 2026-09-09: Bootstrap development dependencies with npm and `--ignore-scripts`; publish only registry-contained dependency closures, never external runtime dependencies (implementation prompt; proposal §5).
- 2026-09-09: Treat the design exercises as historical reasoning wherever the final schemas, suites or later ADRs override them; preserve accepted records unchanged (ADR 0001).
- 2026-09-09: Stop before claiming child descriptor isolation: a real sandboxed reproduction forwards a close-on-exec kernel-like socket to a child; leave the guarantee decision Proposed in ADR 0021 (TE-024).

- 2026-09-09: The operator accepted ADR 0021: ordinary children inherit no authority; deliberate delegation shares the original run principal and fencing. TE-024 is clarified accordingly; implementation resumed.
- 2026-09-09: Finalize spill files with an atomic hard-link creation followed by temporary-name removal, so a repeated call id cannot overwrite an existing artifact (TE-016).
- 2026-09-09: Settle and acknowledge final usage before yielding stop so a client that closes at stop cannot suppress attribution; the same final counters remain observable after stop (PR-011).
- 2026-09-09: TE-001 permits no retrieve handler, so the base loop has an empty fallback and does not require a stage/retrieve provider; this also avoids a core–retriever requirement cycle.
- 2026-09-09: ADR 0022 restricts ephemeral context appends to harness/history; system/skills remain immutable prefix-owned sections, preserving TE-009 and TE-011.
- 2026-09-09: Keep universal and activated skill entries present without `body` when the body budget is exhausted; retain their description as an ignored extension, satisfying SK-010 and TE-008 without exceeding TE-005.
- 2026-09-09: Resolve file symlinks to the destination grant; a read may cross from rw to ro but a write may not, and dangling links are refused (TE-019).
- 2026-09-09: ADR 0023 restores a fenced generation's pins and snapshot under a fresh number; rollback never revives old or failed-candidate credentials.
- 2026-09-09: ADR 0024 supplies schema-defined assistant tool-call history through provider and turn-events contract 1.1.0; the copied 1.0 schemas remain the historical baseline.
- 2026-09-09: Compaction retains an assistant call and its tool results as a group whenever either survives; history limits remain a projection and cannot strand a tool result (ADR 0024).
- 2026-09-09: The provider socket uses a separate schema-defined `{v, runToken}` prelude before describe or one request stream; credentials never enter model.begin, conversation history or vendor requests. The design requires token presentation but leaves its framing unspecified.
- 2026-09-09: A writable grant requires filesystem capacity no larger than its declared storage budget; the runner refuses ordinary unbounded directories. Fixed-size volumes or bounded filesystems supply the quota without filesystem-specific kernel logic (ADR 0005 §4).
- 2026-09-09: The test supervisor receives delegated cgroup controls and writable private procfs to launch real nested sandboxes; candidate processes receive neither those controls nor writable procfs (TE-024).
- 2026-09-09: Compose the socket frame and method-parameter schemas into one cached entry validator; associate the inherited endpoint with its credential in kernel memory and recheck fencing on each operation (KS-001, KS-021).
- 2026-09-09: Correct provider conformance prose that retained the removed door and kernel model rewriting; ADR 0019 and PR-015 place content handling and model.end usage in the unprivileged path.
- 2026-09-09: Reserve compatible calls conservatively at the declared full-context and maximum-output prices; missing prices refuse before HTTP, and OpenRouter receives price ceilings. Explicit breakpoint policies beyond the vendor limit refuse rather than silently changing anchors (PR-002, PR-010).
- 2026-09-09: Derive provider call ids from the persisted conversation head, so tool iterations and process reopenings cannot reuse a turn counter's accounting id (PR-011).
- 2026-09-09: Deliver spawn secrets through descriptor 6 to a fixed bootstrap inside bubblewrap; set only permitted environment names before importing the entry point, keeping values out of bwrap arguments and filesystem mounts (PR-013).
- 2026-09-09: Issue one provisional credential per target for private startup controls; ordinary calls remain refused until fencing commits that generation, and abandoned credentials are revoked before retrying the same number (GN-003–004).
- 2026-09-09: Transfer ownership of the child endpoint at successful spawn: pause the parent's reader before descriptor duplication and close its copy afterward, so it cannot consume child traffic or conceal child EOF (KS-001).
- 2026-09-09: ADR 0025 records commitment intent on entry to SWITCHING and fences before endpoint rename; recovery after that durable point consumes a fresh number even when rename has not completed.
- 2026-09-09: Wait for cgroup.events to report populated=0 before removing a killed process group; the monitor's exit alone does not prove all descendants have left (GN-001, KS-017).
- 2026-09-09: ADR 0026 probes with shared grants read-only, then launches a fresh serving process after commitment; provisional credentials remain probe-only permanently, superseding the earlier commitment-time admission choice.
- 2026-09-09: Retain verified pin copies for undo, compare state trees in bounded workers, and deliver discarded paths and interrupted conversation ids in env.updated; the runtime must persist their conversation notices (GN-001, GN-006).
- 2026-09-09: Keep generation admissions closed throughout driver orchestration and report post-fence failures through fresh-epoch recovery; actual sandbox tests cover migrations, format rejection, both drain modes and undo (GN-001–006).
- 2026-09-09: A pipe error after its write has finished is peer shutdown, not failed credential delivery; preserve short migration exit status instead of racing it with SIGKILL (GN-002).
- 2026-09-09: Reserve four request, handler and queued-frame slots plus 64 KiB of queued bytes for control; bulk saturation cannot consume health, cancellation or stop capacity (KS-017–018).
- 2026-09-09: ADR 0027 keeps package evaluation and init in the loop worker while the environment monitor owns deadlines; a failed package is excluded before restarting initialization of the remaining profile.
- 2026-09-09: Render initialization gaps with cap/init.within-probe for a timeout and cap/init.valid for a rejected initialization; retain the concrete failure separately for diagnosis (TE-022).
- 2026-09-09: Collect pure init registrations before admitting stages; the kernel must still authorize the concrete requirements and spawns before runtime activation (ADR 0016).
- 2026-09-09: Generate internal package-message types from their own schema with the same stale-file gate as the four public contracts; use the existing contract definitions for embedded notices (ADR 0006).
- 2026-09-09: Correct the compatible adapter manifest's network mode from outbound to ADR 0005's egress vocabulary; discovery validates none, egress and mount.
- 2026-09-09: Tag green commit d8aa0d1200c7c24a0fa7f671c41eece204b377fe as the local socket 1.0 compatibility baseline; implementation minor 1.1 runs the same negotiated transport checks with its client and kernel in both directions, without claiming an earlier deployment release (KS-003).
- 2026-09-09: Count every authenticated final usage report; a caller-chosen call id is correlation metadata and cannot suppress accounting for a second vendor call. Only granted callers of a deployment run can contribute reviewed cost to the mount backstop (KS-012–013, ADR 0020).
- 2026-09-09: Persist provider reservations before vendor access; on restart, unfinished reservations remain conservatively charged until their window expires. Persist only person ids and budget totals, never run credentials; production assembly must supply an epoch-based clock and the checkpoint (PR-010).
- 2026-09-09: A failed reservation write prevents vendor access; a failed settlement write retains the earlier durable reservation and refuses further calls. Schema entry validation rejects non-finite numeric values (ADR 0020).
- 2026-09-09: Keep provider admission within the 10-second probe budget until the complete request arrives, then allow a separate 600-second exchange budget; drain closes admission and reports deadline failure after 30 seconds so only the generation machine may replace the process (PR-001, ADR 0010).
- 2026-09-09: Shared service startup reads its budget rule from the inherited endpoint's profile.get response, separately from adapter settings; the service owns an epoch-clock checkpoint in its isolated state (PR-010, ADR 0020).
- 2026-09-09: Quiescing pauses service admission without removing its listening endpoint; an announced env.updated resume reopens admission after a rollback, while health waits for startup and drain acknowledgement (GN-004).
- 2026-09-09: Encode a successful void socket operation as result:null; omitting result violates the response schema and prevented real usage-report acknowledgements (KS-021).
- 2026-09-09: Merge exported spawns with init registrations before activation; bound the combined list, reject duplicate ids and enforce one envelope and declared secret references for both sources. Omitted args and env mean empty collections, matching the evaluator's documented spawn (ADR 0016).
- 2026-09-09: Materialize adapter defaults from its package settings schema through the same shared helper as discovery; reject policy fields in adapter settings and keep the declared LLM_KEY spawn delivery separate (PR-010, PR-013).
- 2026-09-09: A worker error, unexpected exit or initialization-only message after ready fails environment health and resolves its lifecycle result; it never triggers an unannounced reinitialization (TE-022, ADR 0027).
- 2026-09-09: Persist scoped conversation metadata beside JSONL history and sync its parent directory before acknowledging creation; only locally recorded ids may load a conversation (KS-004).
- 2026-09-09: Session submission returns conversation/head metadata, leaving provider output on gateway events; quiescence drains both creation and turns before a snapshot (GN-001, ADR 0019).
- 2026-09-09: Bound session leases to eight one-MiB histories, eight read operations, 32 writes and 64-KiB inputs; release loaded history after its last reader or turn, and reload the stored prefix without rendering it (TE-009).
- 2026-09-09: Generate internal package-state types alongside contract and library schemas; distinguish an internal library named provider from the public provider contract when generating reference imports (ADR 0006).
- 2026-09-09: Reuse the bounded socket transport for monitor/worker session control over a one-peer private endpoint; the monitor retains the inherited kernel endpoint, and worker startup credentials remain in same-process memory (ADR 0027, ADR 0019).
- 2026-09-09: Gate monitor calls until worker initialization and negotiation complete; unexpected control-channel closure fails health. Keep session.submit's deadline separate from the shorter health probe deadline (KS-004, GN-001).
- 2026-09-09: Derive the environment runtime's person and token from inherited authority, ignoring configuration claims; delay session admission until initialization and registration acknowledgements settle (KS-001, KS-004).
- 2026-09-09: Raise the test supervisor's task ceiling to 128 for two environments and a shared service plus test infrastructure; retain the per-sandbox 64-task ceiling (KS-004).
- 2026-09-09: Close the kernel's duplicate client descriptor immediately after sandbox handoff so an actual process crash closes its control channel without a timer (KS-001).
- 2026-09-09: Journal each admitted session submission before dispatch and its completion through the recovery reserve; generate correlation ids in the kernel and observe elapsed time and transport outcome without reading candidate content (ADR 0019).
- 2026-09-09: Summarize stage rows by source, event and outcome with counts and total duration; retain per-iteration retrieval/offer metadata and per-call outcomes. Bound one report to 60 KiB, hash identifiers above 256 bytes, and report a budget error instead of silently truncating diagnostics (ADR 0014, ADR 0019).
- 2026-09-09: Forward one turn.report note through the worker control channel and inherited kernel endpoint before completion metadata; bind journal provenance and target at the authenticated receiver. Late asynchronous hook rejections cannot append to a later turn's report (ADR 0014, ADR 0027).

- Deployment service caller grants name the stable target id; the separate generation field fences credentials, so directory-mounted callers follow a reviewed replacement without losing accounting authority.
- The published endpoint has its own read-only directory; mounting it follows atomic replacement while keeping private generation state outside caller sandboxes.
- Spawn secret requirements strip the `secret/` namespace prefix exactly once when selecting the encrypted store name; the chosen person or deployment scope never falls through.
- 2026-09-09: A trusted run may carry a cost ceiling in USD in addition to the deployment's daily rule; provider reservations persist both ceilings under hashed run identifiers before vendor access, including across provider restart (PR-010, EV-001).
- 2026-09-09: Accepted evaluation evidence is immutable per digest and baseline; re-evaluation against a later baseline creates separate evidence, and a prepare code cannot acquire replacement evidence (KS-014–016).
- 2026-09-09: Recovery checkpoints retain verified copied pins and neutral process plans; ordinary restart reads observed history and consumes a fresh epoch. A retained default transaction manifest selects old pins only through the observed restart transition (ADR 0030).
- 2026-09-09: Stop-mode shutdown runs after durable SWITCHING intent and fencing, before any writable candidate starts; shutdown writes discarded from the snapshot are audited. Shared-mode shutdown runs after old connections drain (TE-023).
- 2026-09-09: Payload V8 old-space is bounded to 32 MiB and semi-space to 1 MiB; snapshot workers use 16 MiB old-space, 2 MiB young-space and a 2 MiB stack. The unchanged 128 MiB cgroup remains the enforced memory boundary; the bounded two-worker pool replaces exited workers for later jobs without replaying failed jobs.
- 2026-09-09: Full immutable-registry assembly uses a separate bounded 512 MiB test filesystem and a 1,536 MiB test supervisor; ordinary test temporary storage remains 64 MiB and individual sandbox quotas remain unchanged. Profiling those sizes is deferred by the user, not their enforcement.
- 2026-09-09: A stopped kernel store export omits socket inodes and symlinks to its own sockets, rejects every other non-regular entry, and retains bounded host-root relocation metadata. Loaders relocate known host paths without changing immutable journal rows or snapshot hashes (GN-007).
- 2026-09-09: A generation may reuse its own previously verified immutable pin copy when both hash and mount match; only the driver supplies retained copies, never a package request. Changed pins still copy and verify before admission (GN-002).
- 2026-09-09: The inherited profile binds the active generation before the environment admits sessions. Per-conversation prefix-generation metadata makes the first turn after that announced change refresh once; ordinary turns and conversation reloads reuse the stored prefix (TE-009–010).
- 2026-09-09: The operator explicitly lifts the earlier UI deferral and approves ADR 0038; work on `gateway-web`'s assets begins in this continuation. There is no `gateway-front` package: each person's `gateway-web` target serves the lifted assets on its own public socket, and the operator's TLS endpoint maps `/login` and `/<person>/` to the matching target by static rule. This is a new approval, separate from and later than the ADR 0037 approval.
- 2026-09-09: A deployment-scope front splicing bytes to each person's `gateway-web` socket was proposed and refused: `kernel/boundary/runtime.ts:265` forbids a target mounting a person-scope service it does not own, and weakening that boundary rule was not offered as a tradeoff (ADR 0038).
- 2026-09-09: D3 — the web surface's cookie is `thetis_session`, already set by `gateway-login/server.ts`; `__Host-thetis` remains the trusted kernel origin's own cookie and is never read or set by `gateway-web`, `gateway-login` or any other package (ADR 0038).
- 2026-09-09: D4 — the lifted UI adapts to the existing `gateway-web` wire rather than the wire adapting to it; the only wire change on `hello` is replying with a `user` frame (ADR 0038).

- 2026-09-10: Document the existing 2,048 MiB, 256-task test supervisor required by the two-account web deployment; per-sandbox limits remain separate from this test-infrastructure budget (KS-004).

- 2026-09-10: The operator requested `@/` imports and selected the runtime repository root. ADR 0047 adds matching TypeScript paths and source/artifact resolution hooks, keeps independently versioned package internals relative, and extends the package import gate.
